#pragma once

#include "khronos_common/CommonDefine.h"

namespace khronos_common {
namespace art {

class LeafNode
{
public:
    LeafNode();
    ~LeafNode();

private:
    AUTIL_LOG_DECLARE();
};

KR_DEFINE_SHARED_PTR(LeafNode);

} // namespace art
} // namespace khronos
